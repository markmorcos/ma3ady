# Tasks

- [x] 1.1 `make migrate-new NAME=availability` → `supabase/migrations/003_availability.sql`
- [x] 1.2 Created `availability_rules` table with full schema (`tenant_id`, optional `service_id`, `day_of_week 0-6`, `start_time/end_time`, `valid_from/valid_until`) and `end_time > start_time` check
- [x] 1.3 Created `availability_exception_kind` enum: `block | extra`
- [x] 1.4 Created `availability_exceptions` table with `ends_at > starts_at` check
- [x] 1.5 Indexes: `(tenant_id, day_of_week)` + `(tenant_id, service_id)` on rules, `(tenant_id, starts_at)` + `(tenant_id, service_id, kind)` on exceptions
- [x] 1.6 RLS enabled on both tables
- [x] 1.7 RLS policies on `availability_rules`: select public, insert/update/delete owner-or-admin
- [x] 1.8 RLS policies on `availability_exceptions`: same shape
- [x] 1.9 `compute_available_slots(text, uuid, timestamptz, timestamptz) returns table(starts_at, ends_at)` — `language plpgsql stable security definer` (per spec; design.md said `invoker` but spec wins). Body for THIS change: tenant resolve, rule expansion in tenant TZ via `(date::timestamp + start_time) at time zone tenant.timezone`, extras union, blocks subtraction via `tstzmultirange` `-` operator. **Tiling into per-service slots, buffers, min_notice/max_advance filtering, daily_cap, and the appointments anti-join are deferred to `define-services-and-appointments` via `create or replace function` with the same signature** — that's the explicit handoff the proposal called out.
- [x] 1.10 Generated types: `pnpm exec supabase gen types typescript --local 2>/dev/null > src/types/database.ts`. Added aliases to `src/types/db.ts`: `AvailabilityRule`, `AvailabilityException`, `AvailabilityExceptionKind`, `AvailableSlot`.
- [x] 1.11 `src/services/api/availability.ts` — `getAvailableSlots({ tenantSlug, serviceId, from, to })` calls the RPC.
- [x] 1.12 Seed (in `supabase/seed.sql`, not `preview-seed.sql` per the local/preview convention): demo tenant Mon–Fri 09:00–17:00 + a Christmas Day 2026 block exception. Used `generate_series(1, 5)` over `day_of_week` to write the five rules in one row insert.
- [x] 1.13 SQL tests at `supabase/tests/availability.test.sql`. Run via `make test-db`. Covers:
  - Rule expansion in tenant TZ on a mid-June Monday (Berlin CEST → UTC+2)
  - DST: pre-spring-forward Monday 2026-01-19 (CET → UTC+1) and post-spring-forward Monday 2026-03-30 (CEST → UTC+2)
  - Block exception splits a single rule window into two
  - Extra exception adds a window outside the weekly grid
  - Anon role can call `compute_available_slots` (SECURITY DEFINER works as designed)
  - Anon role cannot insert into `availability_rules` (RLS works)
  - Service constraints (`min_notice_min`, `max_advance_days`, `daily_cap`, buffers) and appointment anti-join tests live in the next change since those depend on tables that don't exist yet.
- [x] 1.14 Top-of-file comment in `003_availability.sql` documents the function: signature, behavior, deferred parts, and example call (`select * from compute_available_slots('demo', null, now(), now() + interval '7 days')`).
