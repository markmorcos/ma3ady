# Tasks

- [x] 1.1 `make migrate-new NAME=services` → `supabase/migrations/004_services.sql`
- [x] 1.2 `services` table with full schema and constraints (`duration_minutes > 0`, non-negative buffers, `min_notice_min >= 0`, `max_advance_days > 0`, `daily_cap > 0` when set). Also back-fills the deferred FK on `availability_rules.service_id` and `availability_exceptions.service_id` now that `services` exists.
- [x] 1.3 RLS on `services`: select where `active = true` OR member of tenant; insert/update/delete owner-or-admin
- [x] 1.4 `make migrate-new NAME=appointments` → `supabase/migrations/005_appointments.sql`
- [x] 1.5 `appointment_status` enum: `pending | confirmed | cancelled | completed | no_show`
- [x] 1.6 `guest_contacts` with `unique(tenant_id, email)` + `claimed_by_user_id` reference + locale check
- [x] 1.7 `appointments` table with the EXCLUDE constraint (`exclude using gist (tenant_id =, service_id =, tstzrange(starts_at, ends_at) &&) where (status not in ('cancelled','no_show'))`), the `appointments_identity_xor` check, four indexes
- [x] 1.8 `appointment_events(id, appointment_id, event_type, payload jsonb, by_user_id, created_at)` audit table
- [x] 1.9 `handle_appointment_status_change()` trigger function (`security definer`) writes `created` on insert; `status_changed` on status diff; `rescheduled` on `starts_at`/`ends_at` diff. Captures `auth.uid()` as `by_user_id`.
- [x] 1.10 Trigger `on_appointment_status_change` `after insert or update` on `appointments`
- [x] 1.11 RLS enabled on all three tables
- [x] 1.12 `appointments` policies: select for self (`user_id = auth.uid()`) or staff+; insert denied (must go through `book_appointment`); update for staff+; delete denied (cancellations are status updates)
- [x] 1.13 `guest_contacts` policies: select for tenant member or claimed user; insert/update/delete all denied at policy level (only via `book_appointment` SECURITY DEFINER function)
- [x] 1.14 `book_appointment(text, uuid, timestamptz, text, text, text)` SECURITY DEFINER returning `(appointment_id, manage_token)`. Resolves tenant + service, computes `ends_at`, calls `compute_available_slots` to verify the slot is bookable, upserts `guest_contacts` by `(tenant_id, email)`, generates a 32-byte URL-safe-base64 token, stores `sha256` hash. Maps `exclusion_violation` → `slot_taken` error.
- [x] 1.15 `verify_manage_token(text) returns uuid` SECURITY DEFINER. Hashes input, looks up `manage_token_hash`, returns appointment id only when not cancelled; raises `appointment_unavailable` otherwise.
- [x] 1.16 Replaced `compute_available_slots` body via `create or replace function`. Now performs full slot tiling per service (window expansion → buffer-aware tiling via `generate_series` + `make_interval` arithmetic → min_notice/max_advance filtering → appointments anti-join → daily_cap via `row_number() partition by tenant-TZ-date`). Same signature as the partial body shipped in `define-availability-rules`. Now requires `p_service_id`; the legacy null-service "raw windows" mode is gone since the spec only describes booking-flow usage.
- [x] 1.17 Regenerated types: `pnpm exec supabase gen types typescript --local 2>/dev/null > src/types/database.ts`. Added aliases to `src/types/db.ts`: `Service`, `ServiceInsert`, `GuestContact`, `Appointment`, `AppointmentStatus`, `AppointmentEvent`, `BookingResult`.
- [x] 1.18 `src/services/api/services.ts` — `getActiveServices(tenantSlug)` (resolves tenant id then filters by `active = true`), `getService(id)`
- [x] 1.19 `src/services/api/appointments.ts` — `getMyAppointments`, `getAppointment(id)` (RLS scopes automatically)
- [x] 1.20 `src/services/api/booking.ts` — `bookAppointment({ tenantSlug, serviceId, startsAt, guestName, guestEmail, guestPhone? })` wrapping the RPC. Throws typed `SlotTakenError` and `SlotUnavailableError` for the canonical failure modes.
- [x] 1.21 SQL tests at `supabase/tests/booking.test.sql`. Covers slot tiling (16 slots over 8h with 30min duration, no buffers), `book_appointment` happy path, EXCLUDE-induced `slot_taken`/`slot_unavailable` on duplicate, cancelled-frees-the-range, guest_contacts upsert by `(tenant_id, email)`, `verify_manage_token` round-trip + raises after cancellation, RLS denies appointment delete, trigger writes `created` + `status_changed` events. Existing `availability.test.sql` updated to provision a service fixture and verify slot semantics (DST CET/CEST cases preserved).
- [x] 1.22 `supabase/seed.sql` — two demo services ("Consultation" 30min and "Long Session" 60min with daily_cap 4 + 15min buffer-after). Existing demo tenant + Mon–Fri rules + Christmas block preserved.
