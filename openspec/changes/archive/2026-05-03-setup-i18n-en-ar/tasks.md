# Tasks

- [x] 1.1 Install deps: `pnpm add i18next react-i18next expo-localization @react-native-async-storage/async-storage` (versions snapped to SDK 54 via `expo install`)
- [x] 1.2 Write `src/i18n/locales/en.json` with namespaces: `common`, `errors`, `nav`, `auth`, `booking`, `admin`
- [x] 1.3 Write `src/i18n/locales/ar.json` with parity to `en.json` (every key present, native Arabic translations)
- [x] 1.4 Write `src/i18n/index.ts`:
  - module-load: `i18next.use(initReactI18next).init({ lng: 'en', resources: { en, ar }, fallbackLng: 'en', interpolation: { escapeValue: false } })`
  - export `bootstrapI18n()` that reads `AsyncStorage['app.lang']` → `expo-localization.getLocales()[0].languageCode` → `EXPO_PUBLIC_DEFAULT_LOCALE` → `'en'`, then calls `i18next.changeLanguage(resolved)` and triggers RTL bootstrap
- [x] 1.5 Write `src/i18n/i18next.d.ts` declaring `import 'i18next'` module augmentation with typed `defaultNS` and `resources`
- [x] 1.6 Write `src/i18n/rtl.ts`:
  - `RTL_LANGUAGES = new Set(['ar'])`
  - `applyRTL(lang)` checks `I18nManager.isRTL` vs desired, calls `I18nManager.forceRTL(...)` if mismatch, sets `AsyncStorage['app.rtlBootstrapped']`, calls `Updates.reloadAsync()` (dev: `DevSettings.reload()`)
  - First-time switch from EN to AR triggers exactly one reload; subsequent app boots are stable (gated on stored direction)
- [x] 1.7 Write `src/hooks/useLocale.ts`:
  - `useLocale(): { lang: 'en'|'ar', setLang: (l) => Promise<void> }`
  - `setLang` persists to AsyncStorage, calls `i18next.changeLanguage`, calls `applyRTL`
- [x] 1.8 Wire boot phase: `defaultRunners.i18n` now calls `bootstrapI18n()` (the boot sequence in `app/_layout.tsx` already runs that phase before splash hides — see setup-app-shell)
- [x] 1.9 Add `.env.example` entry: `EXPO_PUBLIC_DEFAULT_LOCALE=en`
- [x] 1.10 Add a `/dev/i18n` screen behind the dev-tools gate: shows current lang + `I18nManager.isRTL`, lets you toggle to test RTL
- [x] 1.11 Jest test verifying `t('common.cancel')` returns `'Cancel'` in en and `'إلغاء'` in ar
- [x] 1.12 Jest test verifying every leaf key in en.json has a counterpart in ar.json (parity check, both directions)
- [x] 1.13 Plus: implement the `i18n/no-literal-string` ESLint rule the spec demands — at `eslint-rules/no-literal-string.js`, RuleTester-tested. Scoped to `src/components/**` and `app/**` excluding `app/dev/**` (developer-only English).
- [x] 1.14 Verify on simulator: launching with `EXPO_PUBLIC_DEFAULT_LOCALE=ar` → app boots in Arabic → after one reload, layout is RTL — **deferred to user**
