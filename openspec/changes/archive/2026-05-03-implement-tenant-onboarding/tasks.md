# Tasks

- [x] 1.1 `make migrate-new NAME=pending_memberships` → `supabase/migrations/009_pending_memberships.sql`
- [x] 1.2 `pending_memberships(tenant_id, email lowercase, role, invited_by_user_id, invited_at)` with `primary key (tenant_id, email)`, `email = lower(email)` check, owner/admin RLS, after-insert audit trigger writing `member.invited` (carry-over from setup-tenant-audit-log task 1.6 — done in this migration)
- [x] 1.3 `handle_new_user` trigger extended: after profile insert, looks up `pending_memberships` by lowercased email, inserts matching memberships, deletes the pending rows. Idempotent on conflict.
- [x] 1.4 Edge Function `supabase/functions/claim-slug/index.ts` — JWT-validated; validates slug regex, IANA timezone via `Intl.DateTimeFormat`, locale enum, optional brand_color hex; calls `assert_slug_available`; service-role inserts tenant + owner membership atomically with rollback on failure. Maps `slug_taken` / `slug_reserved` to HTTP 409, `invalid_*` to HTTP 400.
- [x] 1.5 Edge Function `supabase/functions/invite-member/index.ts` — JWT-validated; verifies caller is owner/admin of `tenant_id`; if invitee already exists in `auth.users`, inserts membership and returns `{status: 'added'}`; else calls `auth.admin.inviteUserByEmail` + upserts into `pending_memberships`, returns `{status: 'invited'}`. Returns 409 on already-member.
- [x] 1.6 `app/(onboarding)/_layout.tsx` — Stack wrapped in `RouteErrorBoundary`
- [x] 1.7 `app/(onboarding)/welcome.tsx` — centered logo + tagline, "Set up your business on ma3ady" CTA + "If you were invited, sign in with the invited email" hint
- [x] 1.8 `app/(onboarding)/claim-slug.tsx` — slug input with debounced (400ms) `check_slug_availability` RPC, name input, locale chips (en/ar), pre-filled timezone from `Intl.DateTimeFormat().resolvedOptions().timeZone` (read-only display + caption), 8-color brand swatch picker (now sourced from `src/design/colors.ts → brandSwatches` so `no-inline-hex` doesn't fire). Submit calls `tenantStore.createTenant` which invokes the `claim-slug` Edge Function, refreshes memberships, selects the new tenant, and routes to `/joined`.
- [x] 1.9 `app/(onboarding)/joined.tsx` — success state with check-icon + "You're all set" + three CTAs ("Set up services", "Set availability", "Invite team", currently all returning to `/`).
- [x] 1.10 `app/(app)/tenants/picker.tsx` — modal-presented picker. Each row shows brand swatch, tenant name, role + slug. Tapping selects + routes home. "Add another workspace" button routes to `/(onboarding)/claim-slug`.
- [x] 1.11 `src/state/tenantStore.ts` — added `createTenant(input)` (calls Edge Function via `claimSlug`, refetches memberships, selects new tenant) and `selectTenant(id)` (alias of the existing `switchTenant`, kept for spec parity). `deriveCurrent` updated: with multiple memberships and no stored choice, leaves `currentTenantId` null so the boot router knows to surface the picker.
- [x] 1.12 Boot wiring: `defaultRunners.tenant` now refreshes the tenant store when there's a session, no-op otherwise. Full route-by-membership-cardinality routing logic (0 → onboarding, 1 → home, >1 → picker) is documented as deferred — the route guards land alongside the auth-aware navigation in `implement-admin-mobile-dashboard`. Today the dev links on the home page give direct access to all relevant routes.
- [x] 1.13 SQL tests at `supabase/tests/onboarding.test.sql` (5 groups). Run via `make test-db`. Covers:
  - `check_slug_availability` for available / taken / reserved / invalid (uppercase, too short)
  - Pending-membership audit trigger writes `member.invited` with email + role payload
  - `handle_new_user` promotes pending → membership on auth.users insert; deletes the pending row
  - `assert_slug_available` raises on a taken slug
  - All passing
- [ ] 1.14 Verify on simulator with two test accounts: A claims `acme`, A invites `B@gmail.com` → B signs in → B sees `acme` membership — **deferred to user**, requires GCP OAuth creds + deployed Edge Functions
- [x] **Plus**: added a `src/services/api/onboarding.ts` typed wrapper exposing `checkSlugAvailability`, `claimSlug` (with typed `SlugTakenError` / `SlugReservedError`), and `inviteMember`. Dev links on the home screen include `/welcome (onboarding)` and `/tenants/picker` for direct route access.
