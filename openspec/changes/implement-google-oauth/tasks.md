# Tasks

- [ ] 1.1 Create Google Cloud project + OAuth client (Web application type), set authorized redirect URI to `https://<supabase-project-ref>.supabase.co/auth/v1/callback`
- [ ] 1.2 Update local `supabase/config.toml`: `[auth.external.google] enabled = true, client_id = "..."  client_secret = "env(GOOGLE_CLIENT_SECRET)"`
- [ ] 1.3 In `make dev-up`, ensure `GOOGLE_CLIENT_ID` and `GOOGLE_CLIENT_SECRET` are sourced from `.env.local`
- [ ] 1.4 For preview/prod: `supabase secrets set --project-ref <ref> GOOGLE_CLIENT_ID=... GOOGLE_CLIENT_SECRET=...` (documented; actual call deferred to the `setup-deployment-pipelines` change)
- [ ] 1.5 Install: `pnpm add expo-auth-session expo-web-browser expo-crypto`
- [ ] 1.6 Write `src/services/auth/googleSignIn.ts`:
  - `WebBrowser.maybeCompleteAuthSession()` at module level
  - `signInWithGoogle()` builds the Supabase OAuth URL: `${SUPABASE_URL}/auth/v1/authorize?provider=google&redirect_to=${redirectUri}&code_challenge=...&code_challenge_method=S256`
  - Generates a PKCE verifier with `expo-crypto`, stores it in SecureStore
  - Calls `WebBrowser.openAuthSessionAsync(authUrl, redirectUri)`
  - On return, parses `?code=...` from the redirect URL
  - Calls `supabase.auth.exchangeCodeForSession(code)`
  - Returns the session
- [ ] 1.7 Modify `src/state/authStore.ts`:
  - `signInWithGoogle()` orchestrates the flow + sets `session` in state
  - `signOut()` clears SecureStore + zustand
  - `refresh()` reads `getSession()` on app boot, fetches `profiles` row, fetches memberships
  - On first session restore where `profiles.first_signed_in_at` is null, kicks off `claim-bookings` Edge Function
- [ ] 1.8 Write `app/(auth)/_layout.tsx` (Stack)
- [ ] 1.9 Write `app/(auth)/sign-in.tsx`:
  - Centered logo, tagline, "Continue with Google" button
  - "Continue as guest" link that returns the user to whatever booking flow they came from
  - Error toast on failure
- [ ] 1.10 Write `app/auth/callback.tsx` mirroring stminaconnect pattern:
  - Reads `?code=...` from `useLocalSearchParams`
  - Calls `exchangeCodeForSession` with a 10-second timeout
  - Screen-level timeout of 12s to fail-fast UI
  - On success → navigate to `/` (tenant picker if multi-tenant; otherwise the active tenant's home)
  - On failure → return to sign-in with a toast
- [ ] 1.11 Write Edge Function `supabase/functions/claim-bookings/index.ts`:
  - Verifies the caller's JWT, extracts `email` and `user_id`
  - Updates `guest_contacts set claimed_by_user_id = user_id where email = ? and claimed_by_user_id is null`
  - Updates `appointments set user_id = ?, guest_contact_id = null where guest_contact_id in (those updated rows)`
  - Returns count of claimed bookings
  - Uses service role key (server-side); validates JWT to prevent abuse
- [ ] 1.12 Write `src/services/api/claimBookings.ts` invoking the function via `supabase.functions.invoke('claim-bookings')`
- [ ] 1.13 Modify `app/_layout.tsx` to add the auth restore step in the boot sequence
- [ ] 1.14 Add `EXPO_PUBLIC_AUTH_REDIRECT_URI` to `.env.example`:
  - dev: `EXPO_PUBLIC_AUTH_REDIRECT_URI=https://auth.expo.io/@<expo-username>/ma3ady`
  - prod: `EXPO_PUBLIC_AUTH_REDIRECT_URI=ma3ady://auth/callback`
- [ ] 1.15 Tests:
  - PKCE verifier roundtrip
  - `claim-bookings` claims matching guest contacts and re-links appointments
  - Sign-out clears SecureStore
- [ ] 1.16 Verify in Expo Go on physical device: full sign-in works, session persists, sign-out works, second sign-in skips claim-bookings (already claimed)
