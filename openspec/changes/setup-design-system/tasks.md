# Tasks

- [ ] 1.1 Install deps: `pnpm add lucide-react-native react-native-svg @gorhom/bottom-sheet react-native-reanimated react-native-gesture-handler`
- [ ] 1.2 Configure `react-native-reanimated/plugin` in `babel.config.js`
- [ ] 1.3 Write `src/design/tokens.ts` with `colors` (per `project.md` §1b table), `spacing` (8px grid), `radii` (8/12/16/24), `typography` (sizes 12–40), `shadows`, `motion` (easings + durations)
- [ ] 1.4 Write `src/design/theme.ts`: `lightTheme`, `darkTheme`, `Theme` type. Each theme exposes the same shape — components consume `theme.colors.brand[500]`, never the raw token map
- [ ] 1.5 Write `src/design/ThemeProvider.tsx`: context + `useTheme()` + system theme via `useColorScheme()`; honor user override stored at `AsyncStorage['app.theme']` ('light'|'dark'|'system')
- [ ] 1.6 Modify `app/_layout.tsx` to wrap children with `<ThemeProvider>` (after `<I18nProvider>`)
- [ ] 1.7 Write `src/components/Text.tsx` — variants: `display | h1 | h2 | body | caption | label`; uses `theme.typography` + `theme.colors.text`
- [ ] 1.8 Write `src/components/Button.tsx` — variants × sizes; min hit target 44×44; loading + disabled states; honors `start`/`end` for icon position in RTL
- [ ] 1.9 Write `src/components/Card.tsx`
- [ ] 1.10 Write `src/components/Badge.tsx` — status enum drives color via `statusColorMap`
- [ ] 1.11 Write `src/components/Input.tsx` — label + error + helper text; full RTL support
- [ ] 1.12 Write `src/components/Icon.tsx` — `<Icon name="calendar" color="brand.500" size={20} />` resolves token to hex
- [ ] 1.13 Write `src/components/EmptyState.tsx`
- [ ] 1.14 Write `src/components/Skeleton.tsx`
- [ ] 1.15 Write `src/components/Sheet.tsx` wrapping `@gorhom/bottom-sheet`
- [ ] 1.16 Write `src/components/Toast.tsx` + `src/state/toastStore.ts` (zustand)
- [ ] 1.17 Write `src/branding/Logo.tsx` (wordmark) consuming `assets/branding/wordmark-{en,ar}.svg` based on `useLocale()`
- [ ] 1.18 Write `src/branding/Mark.tsx` (clock-3 mark) consuming `assets/branding/mark.svg`
- [ ] 1.19 Add ESLint rule `no-inline-hex` (custom or via `eslint-plugin-no-color-literals`)
- [ ] 1.20 Add ESLint rule banning `left`/`right` in `StyleSheet`/`style` props (custom regex rule)
- [ ] 1.21 Add `/dev/design-system` route showing every component in light + dark + en + ar (4 combinations)
- [ ] 1.22 Snapshot tests: `Button` in 3 variants × 2 themes
- [ ] 1.23 Verify on simulator: design system showcase renders correctly in all 4 combos
