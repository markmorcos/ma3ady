# Tasks

- [ ] 15.1 Create `marketing/` directory with subdirs: `public/`, `public/en/`, `public/ar/`, `templates/`, `scripts/`
- [ ] 15.2 Write `marketing/Dockerfile` — `FROM nginx:alpine`, `COPY public /usr/share/nginx/html`, `EXPOSE 80`
- [ ] 15.3 Write `marketing/nginx.conf` — gzip on, cache `*.css|*.js|*.png|*.svg` for 30 days, redirect `www.ma3ady.com` to `ma3ady.com`, custom 404
- [ ] 15.4 Write `marketing/public/styles.css`:
  - CSS variables for the brand palette (mirrors design system tokens but recoded as CSS — no JS)
  - Light + dark via `prefers-color-scheme`
  - RTL overrides under `[dir="rtl"]`
  - Typography: Inter for Latin, IBM Plex Sans Arabic for Arabic, loaded via `@font-face` with `woff2` files in `public/fonts/`
- [ ] 15.5 Write `marketing/public/index.html` (English):
  - `<header class="hero">` with locale switcher links to `/` (en) and `/ar/`
  - Wordmark + tagline ("Booking, simplified.")
  - Two App Store / Play Store badges (visually disabled, alt text "Coming soon")
  - Primary CTA "Try the demo" → `https://demo.ma3ady.com` (the demo tenant landing page from change 16)
- [ ] 15.6 Write `marketing/public/ar/index.html` (Arabic, `<html dir="rtl" lang="ar">`)
- [ ] 15.7 Write `marketing/public/404.html` for both locales (en at `/404.html`, ar at `/ar/404.html`)
- [ ] 15.8 Write `marketing/public/robots.txt` allowing all, pointing to sitemap
- [ ] 15.9 Write `marketing/public/sitemap.xml` listing the homepage in both locales + privacy + terms
- [ ] 15.10 Add brand assets to `public/`: `favicon.ico, favicon.png, apple-touch-icon.png, icon.png, og-image.png` (placeholders until designer)
- [ ] 15.11 Write `docs/legal/en/privacy.md` and `terms.md` — initial drafts
- [ ] 15.12 Write `docs/legal/ar/privacy.md` and `terms.md` — Arabic translations
- [ ] 15.13 Write `marketing/templates/legal.html` — shell with header, content slot, footer, language switcher
- [ ] 15.14 Write `marketing/scripts/render-legal.ts` — Deno task that reads markdown, renders via `marked`, slots into the template, writes to `public/{en,ar}/{privacy,terms}/index.html`
- [ ] 15.15 Write `marketing/deno.jsonc` with the `render-legal` task definition
- [ ] 15.16 Write `marketing/deployment.yaml` — schema version 0.4.7, image `ghcr.io/markmorcos/ma3ady-marketing`, port 80, ingress host `ma3ady.com`, TLS via cert-manager (matches stminaconnect)
- [ ] 15.17 Build the Docker image locally (`docker build marketing/`) and run it (`docker run -p 8080:80 ...`); verify the homepage loads in both locales
- [ ] 15.18 Lighthouse: confirm performance + accessibility ≥ 95 on both locale homepages
- [ ] 15.19 Verify `dir="rtl"` works correctly in the Arabic homepage
