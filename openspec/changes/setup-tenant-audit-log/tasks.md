# Tasks

- [ ] 1.1 `make migrate-new NAME=tenant_audit_events` → `010_tenant_audit_events.sql`
- [ ] 1.2 Create `tenant_audit_event_kind` enum with the full list from `proposal.md`
- [ ] 1.3 Create `tenant_audit_events` table with full schema and indexes
- [ ] 1.4 Enable RLS:
  - `select` policy: caller is owner/admin of `tenant_id` (full); or caller is staff and `target_kind = 'appointment'` (limited subset)
  - `insert` policy: deny all (writes only via SECURITY DEFINER triggers)
  - `update`/`delete`: deny all (audit log is immutable)
- [ ] 1.5 Create generic SECURITY DEFINER helper `record_audit(p_tenant_id, p_kind, p_target_kind, p_target_id, p_payload jsonb)`:
  - Reads `current_setting('app.request_id', true)` to thread the request id
  - Reads `auth.uid()` for `by_user_id` (null if SQL session has no JWT, e.g. cron)
  - Determines `by_kind`: 'user' if `auth.uid()` non-null, 'guest_token' if `app.is_guest_token` GUC set, else 'system'
  - Inserts the row
- [ ] 1.6 Create per-table triggers calling `record_audit`:
  - `tenants` AFTER UPDATE: payload `{before: row_to_json(old), after: row_to_json(new), columns_changed: [...]}` for changed columns only
  - `memberships` AFTER INSERT (`member.added` if `pending_memberships` not involved else `member.invited` already handled by pending insert), AFTER DELETE (`member.removed`), AFTER UPDATE OF role (`member.role_changed`, payload `{from: old.role, to: new.role}`)
  - `pending_memberships` AFTER INSERT (`member.invited`), AFTER DELETE (no event — the insert into memberships will produce one)
  - `services` AFTER INSERT (`service.created`), AFTER UPDATE (`service.updated` plus `service.activated` / `service.deactivated` if `active` flipped), AFTER DELETE (`service.deactivated` is preferred path; if a hard delete happens, log `service.removed`)
  - `availability_rules` and `availability_exceptions`: AFTER INSERT/UPDATE/DELETE
- [ ] 1.7 Modify each Edge Function that mutates these tables to set `app.request_id` GUC at the start of its DB transaction (`set_config('app.request_id', '<uuid>', true)`)
- [ ] 1.8 Modify `manage-appointment` and any flow accepting a guest token to additionally set `app.is_guest_token` GUC so audit rows record `by_kind = 'guest_token'`
- [ ] 1.9 Write `src/services/api/audit.ts` with `getTenantAuditEvents(tenantId, { kinds?, actorId?, targetKind?, targetId?, since?, until?, limit? })`
- [ ] 1.10 Write `app/(admin)/audit-log.tsx` (admin-only tab):
  - Header: filter chips for kind + actor + date range
  - List: each row with kind icon, human-readable label (i18n keys), timestamp, actor name
  - Tap row → bottom sheet with payload diff (`before`/`after` columns highlighted)
  - Empty state with helpful copy
  - Pagination: `limit=50`, infinite scroll
- [ ] 1.11 Add audit log routing entry to `app/(admin)/_layout.tsx` — staff sees a limited variant (only appointment-related; or just hide for staff)
- [ ] 1.12 i18n strings for every `kind` value (en + ar):
  - `tenant.updated` → "Tenant settings updated" / "تم تحديث إعدادات الجهة"
  - ...for each enum value
- [ ] 1.13 `make migrate-new NAME=audit_log_retention`:
  - `purge_old_audit_events()` SQL function deleting rows older than 24 months
  - `cron.schedule('purge-audit-events', '20 3 * * *', 'select purge_old_audit_events();')`
- [ ] 1.14 Tests:
  - Each trigger fires correctly: insert/update/delete mutates table → exactly one audit row appears
  - `record_audit` captures `request_id` when GUC is set and null when not
  - Cross-tenant: admin of tenant X cannot read tenant Y's audit log
  - Staff role's audit log view excludes membership/role changes
  - Retention job deletes >24mo rows and leaves recent rows
  - Audit insert path: trigger inserts succeed even when caller's `select` would be denied (SECURITY DEFINER works correctly)
- [ ] 1.15 Manually exercise on simulator:
  - Owner edits tenant timezone → audit row visible
  - Owner invites admin → row visible
  - Admin promotes staff to admin → row visible (with `from`/`to` payload)
  - Admin edits a service → row visible
  - Admin replaces a day's rules via `bulk_replace_rules_for_day` → multiple audit rows visible
